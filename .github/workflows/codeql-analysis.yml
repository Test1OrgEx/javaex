name: CodeQL analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
jobs:
  call-codeQL-analysis:
    name: CodeQL analysis 
    uses: actions/.github/workflows/codeql-analysis.yml@main
